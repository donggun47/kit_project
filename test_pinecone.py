import os
from pinecone import Pinecone as PineconeClient

# Assuming the user's PINECONE_API_KEY from .env is valid,
# or we just want to look at how list_indexes works in the pinecone version installed.

import pinecone
print(pinecone.__version__)
